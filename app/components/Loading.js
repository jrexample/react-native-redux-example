import React from 'react';
import Spinner from 'react-native-loading-spinner-overlay';

export default () => (
  <Spinner visible cancelable size="large" color="#4591cd" overlayColor="rgba(0, 0, 0, 0.15)" />
);
